/**
 * 微信机器人设置 client plugin, node half.
 *
 * Deliberately empty — the settings section and its controller live in the
 * browser half; the host-side authority is the dsh-host-wechat-bridge row.
 */

/** Host plugin body — registration is composition-owned by the web bundle. */
export function apply(): void {}
